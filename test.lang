def extern vararg printf(arg: *u8);
def extern puts(arg: *u8);

type S struct {
	a: i32,
	b: f64,
};

type P struct {
	a: i32,
	b: i32,
	c: f64,
};

def func1(p1: i32, p2: i32, p3: i32, p4: i32, p5: i32, s1: P): P {
	printf("%f\n", s1.c);
	s: P;
	return s;
}

def export main() {
	p: P;
	p.c = 3;
	func1(1, 2, 3, 4, 5, p);
}
